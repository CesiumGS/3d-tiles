{
  "$schema": "http://json-schema.org/draft-04/schema",
  "title": "Buffer",
  "type": "object",
  "description": "A buffer that points to a uri containing binary data.",
  "properties" : {
      "uri": {
          "type": "string",
          "description": "The uri of the buffer. Relative paths are relative to the file containing the buffer JSON. Instead of referencing an external file, the uri can also be a data-uri.",
          "format": "uriref"
      },
      "byteLength": {
          "type": "integer",
          "minimum": 1,
          "description": "The length of the buffer in bytes."
      },
      "name": {
          "type": "string",
          "minLength" : 1,
          "description": "The name of the buffer. The string is UTF-8 encoded."
      },
      "extensions": {},
      "extras": {}  
  },
  "required": [ "uri", "byteLength" ]
}