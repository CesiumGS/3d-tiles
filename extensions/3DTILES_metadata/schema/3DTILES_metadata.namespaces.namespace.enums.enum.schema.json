{
  "$schema": "http://json-schema.org/draft-04/schema",
  "title": "Enum Object",
  "type": "object",
  "description": "An object to define the data type and values for the enum",
  "properties": {
    "valueType": {
      "type": "string",
      "enum": [
        "INT8",
        "UINT8",
        "INT16",
        "UINT16",
        "INT32",
        "UINT32",
        "INT64",
        "UINT64",
        "STRING"
      ],
      "description": "The data type for the enum."
    },
    "values": {
      "type": "object",
      "description": "A dictionary, where each key is an enum name and each value is an enum value. All values must be of the same type as declared in the `valueType`.",
      "minProperties": 1
    }
  }
}