{
  "$schema": "http://json-schema.org/draft-04/schema",
  "title": "Class Object",
  "type": "object",
  "description": "A class defining the properties that distinguish one set of instances from another.",
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "The name of the class, e.g. for display purposes. The string is UTF-8 encoded."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "description": "The description of the class. The string is UTF-8 encoded."
    },
    "properties": {
      "type": "object",
      "description": "A dictionary, where each key is a property ID and each value is an defining the property.",
      "minProperties": 1,
      "additionalProperties": {
        "$ref": "3DTILES_metadata.classes.class.property.schema.json"
      }
    }
  }
}