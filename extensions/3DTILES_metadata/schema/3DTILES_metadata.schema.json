{
  "$schema": "https://json-schema.org/draft-04/schema",
  "title": "3DTILES_metadata object",
  "type": "object",
  "description": "An object containing description of metadata available",
  "properties": {
    "tileset": {
      "$ref": "3DTILES_metadata.tileset.schema.json"
    },
    "classes": {
      "type": "object",
      "description": "A dictionary, where each key is a class ID and each value is an object defining the class.",
      "minProperties": 1,
      "additionalProperties": {
        "$ref": "3DTILES_metadata.classes.class.schema.json"
      }
    },
    "classesUri": {
      "type": "string",
      "minLength": 1,
      "description": "A uri to an external JSON resource containing the classes. The external JSON resource must follow the same schema as `classes`. `classes` and `classesUri` cannot both be supplied. If neither `classes` nor `classesUri` is supplied the classes should be supplied through some other means."
    },
    "enums": {
      "type": "object",
      "description": "A dictionary, where each key is an enum ID and each value is an object defining the enum.",
      "minProperties": 1,
      "additionalProperties": {
        "$ref": "3DTILES_metadata.enums.enum.schema.json"
      }
    }
  },
  "not": {
    "anyOf": [
      { "required": ["classes", "classesUri"] }
    ]
  }
}
