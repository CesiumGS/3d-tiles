{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "conditionalContentItem.schema.json",
  "title": "One item of the conditional content for 3DTILES_content_conditional",
  "description": "Defines the structure of a single content item in 3DTILES_content_conditional",
  "$ref": "content.schema.json",
  "properties": {
    "keys": {
      "description": "Properties that define the conditions based on which the item can become active. The names of these properties correspond to the 'dimensions' that are defined in the top-level extension object.",
      "type": "object",
      "additionalProperties": {
        "type": ["string", "number", "boolean" ]
      }
    }
  },
  "required": [
    "uri",
    "keys"
  ]
}